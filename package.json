{
  "name": "e-commerce-single-page",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"live-server --host=localhost --port=3000\" \"npx tailwindcss -i ./src/input.css -o ./output-style.css --watch\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.0",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.14"
  }
}
